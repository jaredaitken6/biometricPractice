{
  "homepage": "https://jaredaitken6.github.io/biometricPractice",
  "scripts": {
    "build": "if not exist build mkdir build && copy index.html build\\index.html && copy script.js build\\script.js && copy styles.css build\\styles.css && (if exist assets xcopy assets build\\assets /E /I /Y 2>nul)",
    "deploy": "gh-pages -d build",
    "test": "jest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "gh-pages": "^6.3.0",
    "globals": "^16.0.0",
    "jest": "^29.7.0"
  }
}