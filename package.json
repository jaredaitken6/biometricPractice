{
  "homepage": "https://jaredaitken6.github.io/biometricPractice",
  "scripts": {
    "build": "rimraf build && mkdir build && cp index.html build/ && cp script.js build/ && cp styles.css build/ && cp -r assets build/assets/",
    "deploy": "gh-pages -d build",
    "test": "jest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "gh-pages": "^6.3.0",
    "globals": "^16.0.0",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1"
  }
}
